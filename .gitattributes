# ------------------------------------------------------------
# Core normalization: use LF in repo, native line endings on checkout
# ------------------------------------------------------------
*         text=auto

# ------------------------------------------------------------
# Windows scripts: force CRLF in repo to ensure compatibility
# ------------------------------------------------------------
*.ps1     text eol=crlf
*.bat     text eol=crlf

# ------------------------------------------------------------
# C# / MSBuild projects: normalize line endings (LF in repo), 
# but allow native checkout (don't force CRLF)
# ------------------------------------------------------------
*.cs      text
*.csproj  text
*.sln     text
*.props   text
*.targets text

# ------------------------------------------------------------
# Unix shell scripts: enforce LF line endings for consistency
# ------------------------------------------------------------
*.sh      text eol=lf
*.bash    text eol=lf

# ------------------------------------------------------------
# CI/CD configs and GitHub workflows: LF for cross-platform compatibility
# ------------------------------------------------------------
*.yml        text eol=lf
*.yaml       text eol=lf
.github/**   text eol=lf

# ------------------------------------------------------------
# Binary files and secrets: mark as binary to prevent line-ending changes
# ------------------------------------------------------------
*.png     binary
*.jpg     binary
*.jpeg    binary
*.gif     binary
*.pdf     binary
*.pfx     binary
*.p12     binary
*.pem     binary
*.cer     binary
*.crt     binary
*.key     binary


